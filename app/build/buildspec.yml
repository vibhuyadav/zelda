version: 0.2

phases:
  build:
    commands:
      - echo The name of the project - $PROJECT_NAMe
      - echo This is a static website. Nothing to build, lint or test yet.
artifacts:
  files:
      - 'app/src/**'
